auto eth1
allow-hotplug eth1
iface eth1 inet static
{% if gateway.networks.internal.interface == "eth1" %}
        address {{gateway.networks.internal.ip}}
        netmask {{gateway.networks.internal.netmask}}
        dns-search {{gateway.networks.internal.domain}}
        dns-nameservers 127.0.0.1
{% else %}
        address {{gateway.networks.external.ip}}
        netmask {{gateway.networks.external.netmask}}
        gateway {{gateway.networks.external.gateway}}
{% endif %}
