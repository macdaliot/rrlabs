127.0.0.1 localhost localhost.localdomain
::1 localhost ip6-localhost ip6-loopback
ff02::1 ip6-allnodes
ff02::2 ip6-allrouters
{{networks.internal.ip}} {{inventory_hostname.split('.')[0] | lower}}.{{networks.internal.domain}} {{inventory_hostname.split('.')[0] | lower}}
{% for host in networks.internal.hosts %}
{{host.ip}} {{host.hostname}}.{{networks.internal.domain}} {{host.hostname}}
{% endfor %}
